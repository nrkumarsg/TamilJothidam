# Task: Key Life Timeline

Based on the JSON data below, write the "Key Life Timeline" section.

Using `currentDasha` and `dashaTimeline` (if present in the data), list each Mahadasha period in chronological order alongside its lord's house lordship/placement.

## 1. Timeline Summary
If `dashaTimeline` is present, briefly list each Mahadasha with its period and general theme (e.g. growth, challenge, stability). Otherwise, write based only on the current dasha.

**Strictly avoid:** Do not assert that specific events (marriage, birth, death, etc.) will happen on specific dates.
